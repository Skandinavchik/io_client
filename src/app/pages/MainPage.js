


const MainPage = () => {

    const style = {
        position: 'relative',
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    };

    return (
        <main style={style}></main>
    );
};

export default MainPage;